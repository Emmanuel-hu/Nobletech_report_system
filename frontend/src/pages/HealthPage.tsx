const HealthPage = (): JSX.Element => {
  return (
    <section aria-labelledby="health-heading">
      <h2 id="health-heading">Frontend Health</h2>
      <p>NEMP Frontend is running.</p>
    </section>
  );
};

export default HealthPage;
