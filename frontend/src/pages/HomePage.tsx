const HomePage = (): JSX.Element => {
  return (
    <section aria-labelledby="home-heading">
      <h2 id="home-heading">NEMP Frontend Foundation</h2>
      <p>
        This starter shell provides the approved baseline for routing, TypeScript, testing, and
        quality tooling.
      </p>
      <p aria-live="polite">Backend health-check status: Pending integration.</p>
    </section>
  );
};

export default HomePage;
